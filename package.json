{
  "name": "discord-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prestart": "node --experimental-strip-types --env-file-if-exists=.env src/deploy-commands.ts || exit 0",
    "start": "node --experimental-strip-types --env-file-if-exists=.env src/index.ts",
    "start:test": "node --experimental-strip-types --env-file-if-exists=.env.test src/index.ts",
    "send": "node --experimental-strip-types --env-file-if-exists=.env src/send.ts",
    "diag": "node --experimental-strip-types --env-file-if-exists=.env src/diag.ts",
    "deploy": "node --experimental-strip-types --env-file-if-exists=.env src/deploy-commands.ts",
    "deploy:test": "node --experimental-strip-types --env-file-if-exists=.env.test src/deploy-commands.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.5",
    "discord.js": "^14.27.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
